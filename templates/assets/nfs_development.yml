---
name: nfs-development
public: true
serial: true
plan:
- aggregate:
  - get: templates
  - get: nfs-release
  - get: deployments
  - get: ci
- task: vsphere-stemcell
  file: ci/tasks/download.yml
  config: { params: { url: 'https://blobs.example.com/stemcell.tgz' } }
- task: spruce-merge
  file: ci/tasks/spruce-merge.yml
  config:
    params:
      files: templates/nfs/manifest.yml deployments/development/nfs/stub.yml
- put: nfs-development-deployment
  params:
    manifest: spruce-merge/manifest.yml
    releases:
    - nfs-release/*.tgz
    stemcells:
    - vsphere-stemcell/*.tgz
